spring:
  application:
    name: miscale2pg

  mvc:
    problemdetails:
      enabled: true

  threads:
    virtual:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus

app:
  miscale.api:
    base-url: https://api-mifit.huami.com

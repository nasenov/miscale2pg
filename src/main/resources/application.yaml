spring:
  application:
    name: miscale2pg

  docker:
    compose:
      stop:
        command: down
      skip:
        in-tests: false

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
